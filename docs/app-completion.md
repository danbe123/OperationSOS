# App completion work

Working checklist, started 2026-09-05. Software validation and physical-device acceptance are recorded separately.

- [x] Commit the frontend fixes and UX work.
- [x] Merge backend, authored content and maps branches into main.
- [x] Pass the unified repository test target (including updated plan-04 merge).
- [x] Finish map packs, verification and API integration.
- [x] Build and validate real map outputs, install available maps in the running app.
- [x] Switch the development stack to the unified checkout, preserving its database and notes.
- [~] Audit full-library sizes, sources and citation availability; download what fits (core downloads done; seven core items still need building).
- [x] Implement AI retrieval, inference, lifecycle, citations and evaluation (all eval gates pass on the PC; see below).
- [ ] Add app crash recovery and finish cross-screen UX checks.
- [ ] Run full browser and backend checks against the integrated app.
- [ ] Record hardware acceptance on the Pi (requires the physical device).

## Current evidence

- Main includes plan-01 through plan-04 and the frontend work.
- Before integration: 215 frontend component tests passed; browser checks covered navigation, PDF reading, tables, checklists, and responsive layouts.
- First unified backend run: 528 passed, one skipped, two failures caused by duplicate map manifest declarations. Those declarations have been consolidated.
- Map packs and verification: 14 focused tests pass. A real fixture map build is in progress.
- The running development library initially contained eight available items; AI was a 503 placeholder and no base maps were available.
- Physical-device acceptance has not been performed; see hardware-checklist.md.

## Integration update

- Incorporated the later plan-04 commits through 4fc169a in merge c967061, retaining their reviewed map implementation and fixtures.
- Pre-merge unified checks: 627 backend tests and 215 frontend tests passed; 72 authored documents validated. Browser fixture suite: 14 passed, six optional AI/dev tests skipped.
- Development database/configuration copied to main with a SQLite backup under `.dev/backups/20260905-141902`.
- Local Gemma model started successfully; a live request completed with citations. Retrieval evaluation remains below the acceptance gate (0.48 versus 0.80), so AI acceptance is outstanding.
- Full map build produced base/OS/terrain outputs but stopped because no flood-zone source URLs are configured. Do not mark flood coverage available or substitute fixture tiles.
- Core-library dry-run resolved all sources. Downloads and deep citation checks remain outstanding; the extended library exceeds current free disk space.
- Original in-progress work is preserved in stash c84fc969041160788d5c918481221aff615df65a; overlapping map changes were superseded by the reviewed plan-04 implementation.

- Post-merge unified checks: 641 backend tests passed, one skipped; 215 frontend tests passed; all 72 documents and smoke self-test passed.

## 2026-09-05 evening update

- AI implementation (plan 05) committed on main as 12f9024; the unified test target passed beforehand.
- Full map build completed: four base archives and ten overlays verified in `/home/dan/sos-content/maps/build.json`, both OSM and OS bases reported available by `/api/map/config`.
- Core library downloads finished (full Wikipedia at 20:35, size matches the manifest). 129 of 136 core items available. The seven missing core items are all build artefacts, not downloads: prepare_uk, govuk_resilience, legislation_uk (zimit crawls), nhs_uk (build-nhs), the two Hesperian PDFs and the gemma-3-1b fallback model (manual). Disk is 87 percent full; the extended tier still does not fit.
- Five duplicate dated ZIM copies (ifixit, nhs medicines, zimgit medicine/post-disaster/water) verified byte-identical and unreferenced, then deleted.
- Retrieval evaluation against the full library moved from 0.48 to 0.75 with no code change (the library was simply present), then to 0.88 after ranking fixes in `sos.ai.run_search`:
  - title shape: question-form titles (Stack Exchange threads) discounted, topical titles fully explained by the query rewarded, curated playbook titles treated as topical;
  - source authority: relevance multiplied by the manifest search weight (playbooks 1.6), and every Stack Exchange archive set to 0.8 in `manifest/core.json`;
  - stemming fixes (warnings/warning, foxes/fox, poisonous/poisoning) and suggest probes with the raw term as well as the stem (virus);
  - passage windows prefer covering more distinct query words over repeating one.
- Question file changes, all adding an equivalent or better source rather than removing one: a14 full-Wikipedia Rabies, a16 WikEM Mushroom poisoning, a19 Wikipedia Knife sharpening, a23 the iFixit "Sew a Button" guide. Remaining retrieval misses (a11, a12, a15, a17, a20, a27, a48) are synonym gaps or equivalent articles under a different book id; a27 and a48 are judgement calls on the expected list.
- Retrieval-only run recorded at `tools/eval/runs/2026-09-05-retrieval-only.jsonl`: retrieval@3 0.88 (gate 0.80), verbatim 1.00 (gate 0.90).
- Full model-backed eval (gemma-4-E2B-it-Q4_K_M, PC, 21:46): retrieval@3 0.88, verbatim 1.00, refusal 0.91 (gate 0.90), grounded 0.85, median time to first token 17.6 s, 26.7 tokens/s, peak RSS 4.9 GB. Run file `tools/eval/runs/2026-09-05-gemma-4-E2B-it-Q4_K_M.jsonl`. The refusal gate is marginal (10 of 11); Pi timings will differ.
- Home: the "Start here" label and the quick-help shortcut grid were removed at the owner's request.
- Tools expansion designed (`docs/superpowers/specs/2026-09-05-tools-design.md`): stock and household register, children's dose tool, situation clock, timers, sun and moon, calculators, event log.

## 2026-09-05 late: tools expansion built

Per `docs/superpowers/specs/2026-09-05-tools-design.md`, all on main:
- API: household register, stock with days-left from the household size, situation clock (`/situation`, `situation` on `/status`), event notes.
- Web: sixth Home tile Tools; Timers (countdowns, CPR beat at 110, fallout 7:10 marks), Sun and moon, Calculators (generator, battery, solar, rations), Event log, Children's doses (NHS age bands from the library's NHS pages, cited), Plan split into Household, Stock, Household plan, Notes, Pins, Event log; situation clock on playbooks with the current phase tab marked; Medical shows household medical needs and the dose tool.
- Checks: `make test` 665 backend, 251 frontend, 72 documents, smoke OK; Playwright UX spec extended with the tools and clock passes; screens screenshotted on the dev stack at 853x480.
- Restarting the dev API by hand trips `run-dev.sh`'s cleanup trap and stops Caddy, kiwix and llama-server too: restart the whole stack with `SOS_MANIFEST_DIR=.dev/full-manifest dev/run-dev.sh` and re-enable the AI afterwards.

## 2026-09-05 late: content expansion (survival, bushcraft, military field craft, Hesperian)

- Where There Is No Doctor (1992 revised edition, 2003 printing) and Where There Is No Dentist (1983) now come from archive.org copies with hashes, so `sos sync` fetches them like every other document. Hesperian's current printing is sale-only; the archive copies are complete earlier editions and the descriptions say so.
- Added to core (all public domain US Army or Project Gutenberg / pre-1927 scans): ATP 3-50.21 Survival (2018), FM 21-76-1 Survival Evasion and Recovery, FM 21-10 Field Hygiene and Sanitation, FM 4-25.12 Unit Field Sanitation, FM 31-70 Basic Cold Weather Manual, TC 3-97.61 Military Mountaineering, FM 3-25.26 Map Reading and Land Navigation, FM 5-125 Rigging, TC 3-21.76 Ranger Handbook (2017), ST 31-91B Special Forces Medical Handbook; Kephart's Camping and Woodcraft (1917), Seton's Book of Woodcraft (1921), Nessmuk's Woodcraft and Camping, Beard's Camp-Lore and Woodcraft, Kreps's Woodcraft and the 1911 Boy Scouts Handbook (EPUBs from Gutenberg).
- Added Kiwix ZIMs: Canadian Prepper (winter prepping, prepping food, bug-out concepts, bug-out bag; about 7.3 GB), WikiCiv, GrimGrains, Restarters, WikiVet, Quick guides for medicine. S2 Underground and Medicine LibreTexts were already in the extended tier.
- Not added: anything copyrighted and in print (SAS Survival Handbook, Bushcraft 101, Food for Free, Where There Is No Vet has no free copy), the 235 GB Survivor Library (does not fit), and the Kiwix catalogue has no bushcraft, foraging or wikiHow builds in English.
- Map: every overlay feature now shows a hover or tap tooltip that says what it is in plain English (name or type, operator, phone, opening hours, flood zone meaning, access-land designation, right-of-way type).
- Field craft: after the owner questioned the US-military slant, a ten-page UK field-craft set was written in our own words (`playbooks/pages/fieldcraft-*.md`, category `fieldcraft`, shown on `/fieldcraft` from the Tools tile and linked from 21 playbooks and modules). The US manuals stay as depth links and are labelled as such.
- What is working: five shared toggles at the top of Home (power, water, gas, internet, phones; green on, red off; `/api/services`, also on `/status`). Anything off adds an outage panel on Home with the relevant playbooks and pages and a one-line notice on every content screen; with phones off every 999, 112, 111, 105 and 101 in rendered content is struck through with a "phones down" tag linking to the new page Getting help without phones (`playbooks/pages/no-phones.md`).

## 2026-09-06: library pruned to relevance; situation engine under way

- Manifests pruned (commit 14af685): extended is now 37 items (sciences, engineering, K-12 and trades LibreTexts, OpenStax, S2 Underground, Welsh Wikipedia, nine Stack Exchange sites, the owner's shelves, plus the three archives that need the external drive: Survivor Library 252 GB, Gutenberg 221 GB, Khan Academy 180 GB). Core drops DevDocs, the OpenStreetMap wiki, Wikivoyage and Physics Q&A. The curated extended set that fits this PC (21 GB, 30 items) is being fetched into `.dev/extended`.
- Disk: 119 GB free before the extended fetch; `sos-content/maps-src` (22 GB of rebuildable map sources) is the next thing to clear if space is short. The three big archives wait for the SOS-EXT drive.
- The situation engine (spec `2026-09-06-situation-engine-design.md`, plan `docs/superpowers/plans/2026-09-06-situation-engine.md`) is being built by three Opus agents: backend engine and API, frontend phase 1, and a content pass adding condition directives to every playbook, module, card and page.
- Situation engine wave 1 landed (commits 3d5b481, 4cd591f, 588fa99, c9f2986 backend; 7fafe20, 155d585, 2b9591c, dc6d366 frontend; fb123df, 83df46f, 19de555, f66d602, 9a848fa, 3f7169f content): ten conditions with a clock and audit events, 55 cited rules, deterministic engine with golden tests and a 400-step simulator, `/api/situation/view`, conditions, tasks, home, drill and report endpoints, condition directives rendered per situation and validated for every branch (162 directives across 66 documents), the situation strip and briefing on Home, the situation sheet, the tasks screen and chrome chips. The services toggles are gone. `make test`: 931 backend, 324 frontend, 83 documents, smoke OK. Wave 2 (sensors, nearby, Piper, board, modes, map home) is in progress.
- Situation engine wave 2 landed and is live on the dev stack: nearest facilities from the overlays (needs `SOS_MAPS_SRC`, now passed by run-dev), readiness recompute, sensor drivers with detection (internet probe, mains via UPS or GPIO, file sensors, RTL-SDR band scan and bulletin recording), Piper read-aloud (installed, synthesises in under half a second), the kiosk board and idle switch, calls-hidden mode, map home and nearby with routes, readiness gaps, drill banner and debrief, sensors on the sheet, forecast reminders. `make test`: 994 backend, 368 frontend, 83 documents, smoke OK; Playwright 25 passed. Phase 4 (neighbours, street list, export and import, simulator in the test target, hardware notes) and the interface redesign (`docs/superpowers/specs/2026-09-06-interface-redesign.md`) are in progress.
- Situation engine phase 4 landed (neighbours, street list, who to check on, export and import as QR chunks, the simulator in `make test`, hardware notes): backend 1057 tests.
- Interface redesign landed (commits e731a06 to a0a10f1): a shell with a 96 px rail on the kiosk and a bottom bar on phones, five destinations (Now, Guides, Medical, Map, Find), the situation band, one token system in `web/src/styles/tokens.css` with all three themes at 7:1, one task list, 407 frontend tests, 27 browser specs, and 294 screenshots of every inventory entry in `docs/superpowers/critique/round-0/`. Five critique rounds follow (`docs/superpowers/critique/round-N.md`).

## 2026-09-06: hillshade rebuilt; sea masked, Scotland north of 57 N restored

- The map showed a lighter rectangle around the UK and Ireland: the hillshade archive shaded the 0 m sea as flat ground wherever Copernicus GLO-30 had data (11 W to 2.2 E, 49.1 N to 57 N), and the 5 September archive had no relief at all north of 57.3 N (OS Terrain 50 was lost in that run; not reproducible, no log kept). A VRT also reads the gaps between grids as 0 m, so the OS Terrain 50 rectangle flattened the Isle of Man and eastern Ireland on top of Copernicus.
- `sos build-maps --steps hillshade` now builds every DTM VRT with `-vrtnodata -9999` (and opens the OS grids as Float32, since a whole-metre grid opened as Int32 and was dropped), rasterises OSM land polygons (`LAND_POLYGONS_URL` in `install/versions.env`, 925 MB, cached in `maps-src`) on the mosaic's exact grid, and zeroes the shaded sea with `gdal raster calc`. `gdalwarp -cutline` was tried first and rejected: it declares the coastline self-intersecting once transformed and then writes an empty raster with exit 0.
- Rebuilt on the PC in 11 minutes (`maps-src/hillshade-build.log`): 400 MB, 16,505 addressed tiles, coverage follows the coast; Wick, Kirkwall, Lerwick, Inverness, Snaefell and the Dublin hills all shaded; `verify` OK. Backend 1060 tests pass; smoke OK. The dev stack must run with `SOS_MANIFEST_DIR=.dev/full-manifest` or the map page says no base map is installed.

## 2026-09-06: polish pass on the shell and components

- The owner's verdict on the redesign was "not polished": every element was the same 1 px outlined rounded box, panels sat on a ground almost their own colour, the accent green edged everything, titles were barely larger than body text, and three medical cards wore a "?" icon.
- Surfaces now carry the structure: a `--raised` control tone in every palette (dim and print included), panels and secondary controls without borders, borders only on inputs and the chosen state, one 8 px radius, tinted badges, chips and condition tiles (`color-mix` of the state colour into the panel), lines in a neutral green-grey, the rail's active row on the raised tone with a 3 px edge, the search field and its button joined into one control, 26 px kiosk titles, 20 px panel headings. Eighteen icons added (`lungs`, `syringe`, `baby`, `zap`, `atom` and the rest the cards and scenarios name).
- Layout defects fixed on the way: band chips wrapped "off" over "4 h" (now one line, no icon, phones show the count only); a phone's one chip was cut to "Power"; the scenario's tab strip wrapped "Go deeper" and its summary pushed the guidance below the fold on the kiosk in an outage (tabs on one line, summary hidden under 560 px, the head's Find hidden where the rail has it); the board's sixth condition tile was clipped; an orphaned "Steps" heading under step one of every card; the card's shrink measured in the fallback font before Inter loaded; the kiosk keyboard opened over search results on arrival with a query; chip labels cut mid-word.
- Tests updated to the new behaviour: 21 unit tests that described the old card, sheet, band, reader stylesheet and search; the state-button style test; the browser specs for the card, the sheet, the tick and the shell. Frontend 527 unit tests, tsc and eslint clean. Headless Chromium runs on the PC without sudo by unpacking libnspr4, libnss3 and libasound2 next to it (see the project memory note).

## 2026-09-06: two themes, Field and Mono

- The owner's direction was "i only want the light theme, and a black and white power saving one". The dark-green `vault` and the red `blackout` are gone, the light `field` is now the box default and the bare `:root`, and the new `mono` is white on pure black with no hue in any token — an OLED phone draws no current for a black pixel, so it is the theme that costs the least to read on the fourth day of a power cut, and a colourless screen keeps night vision. The three states are separated by luminance (ok #BDBDBD, warn #DEDEDE, danger #FFFFFF) with the ✓ ▲ ✕ already beside them carrying the meaning; links stay underlined.
- The 7:1 floor now covers `--raised` as well as `--ground`, `--panel` and `--sunken`, since every button, tile and chip is filled with it. That moved field's `--signal`/`--ok` from #15532E (6.97:1 on a raised surface) to #144E2B (7.47:1) in the theme and in the print block. The worst measured pair across all four palettes is 7.18:1.
- The engine's night mode now raises `mono` with dim on (`playbooks/rules/modes.yaml`), the API's `THEMES` and `default_theme` are `("field", "mono")` and `"field"`, and the map styles are `osm-field.json`/`osm-mono.json` (Protomaps `light` and `black` flavours) and `os-field.json`/`os-mono.json` (OS Outdoor and Night). The four `*-vault.json`/`*-blackout.json` files were deleted from `~/sos-content/maps/styles` and from the committed fixture build.

## 2026-09-07: no setup

The owner: "i dont like the stock idea, and the adding user information etc … it's meant to be more general, i dont want loads of setting up for the users." Design in `docs/superpowers/specs/2026-09-07-no-setup-design.md`, removal map in `docs/superpowers/plans/2026-09-07-no-setup-context.md`.

- Removed: the household register and its screen, the neighbours list and its street print, Stock and its meter screen, the readiness score and its gaps, and every rule and task that named a person or a neighbour (`needs:`, `skills:`, `who:`, `stock:` on a rule; the schema now rejects them). The Household hub, its sub-screens and the `/plan/*` routes are gone; `/plan` and `/plan/*` fall through to the not-found page. `GET /api/household`, `/api/neighbours` and `/api/stock` and their siblings answer 404. `/status` has no `readiness_score`.
- Added: a single `people` setting (integer, default 2, minimum 1, maximum 20), with a one-tap stepper on the Kit screen and `GET`/`PUT /api/settings/people`. Every kit is now relevant to everybody (no register to gate on) and scales its quantities by this count, including the kits that used to gate on age or needs. Peacetime Now drops the score and the household-and-stock summary for a plain "Everything is working" / "Start here": the kits' basic-tier progress in one line, the drill button and the guide tiles. Notes and pins moved to their own screen at `/notes`; the event log stays on `/situation#log`. Tasks take a typed name instead of an assignment picked from a register. The household plan page stays as content at `/p/household-plan`, in the Guides Reference group, with its meeting-point idea now prose rather than a tracked flag.
- Upgrade note: a box that has already been set up keeps its household, neighbours and stock tables — nothing is dropped, so a later restore stays possible — it just stops showing them; the readiness score is not recomputed and does not appear. An export taken before this cut still carries a household register, a stock list and a street list; this box's transfer omits those parts going forward, and importing an old export ignores the removed parts with a note in its summary rather than failing.
- Verification: API suite 1103 passed; `tsc --noEmit` clean; web suite 555 passed (72 files); `sos validate-playbooks --all-scenarios` OK, 119 documents; no Python files touched by this task, so ruff was not run. `pnpm --dir web build` succeeded; the dev stack restarted with `SOS_MANIFEST_DIR=.dev/full-manifest`; `/api/household` confirmed 404, `/plan` confirmed the not-found page. Screenshots of `/`, `/kit`, `/notes` and `/tasks` at 853x480 and 390x844 are in `docs/screenshots/2026-09-07-no-setup/`.

## 2026-09-14: the library reads like books

The owner: "Books read like a Kindle, not a scanned PDF." Design in `docs/superpowers/specs/2026-09-14-books-reflow-design.md`, plan in `docs/superpowers/plans/2026-09-14-books-reflow.md`. Every library PDF was run through `sos build-books` (Calibre `ebook-convert` with `--enable-heuristics`) on the PC; the EPUB is the item's primary form and the original PDF stays beside it as `pdf_dest`, one manifest row per book.

- Converted: 62 of the 76 PDFs are now `kind: "epub"` (the Approved Documents, the Ship Captain's Medical Guide chapters, the US field manuals with a text layer, six OpenStax science textbooks, the HSE leaflets, the resilience frameworks, Emergency War Surgery, Survival and Austere Medicine, the WHO essential medicines list, the FEMA nuclear detonation guidance). Total EPUB footprint 1.6 GB against 2.6 GB of PDFs.
- Left as PDF, by rule: four OpenStax maths and physical-science titles (Prealgebra, Introductory Statistics, College Physics, Chemistry — their inline equations are vector drawings that no text extractor recovers, so reflowed text loses the numbers) and Ofcom IR2009 (one large table). Left as PDF by the tool's own gate: seven OCR scans whose text layer is invisible to Calibre (Where There Is No Doctor and Dentist, Protect and Survive, NRR 2025, NWSS, FM 21-10, FM 21-76-1), one scan with no text layer at all (ST 31-91B), and two Gutenberg-era scans (Kephart, Seton) that Calibre grinds on for over an hour; those two arrive as proper EPUBs with the Gutenberg collection.
- The tool: `sos build-books` unwraps lines with Calibre's heuristics, retries once with the flow splitter off when Calibre's splitter gives up, fails a conversion that keeps under half of the PDF's words or comes from a PDF with no text layer, reports an unreadable EPUB as a FAIL rather than aborting the batch, and downloads a missing source PDF through a `.part` stage. A converted book is indexed from its original PDF, so search hits and the 805 `#page=` citations keep real page numbers and open the original layout at the right page; the EPUB reader has one compact toolbar so the book gets the screen on the 853x480 kiosk.
- Reader chrome: a converted book opens reflowed with an "Original PDF layout" toggle; a `#page=` link opens the PDF at that page with a "Reflowed text" toggle.
- Next: `docs/superpowers/specs/2026-09-14-gutenberg-library-design.md` (draft, awaiting the owner) makes the Gutenberg collection read the same way, on a 1 TB core standard.

## 2026-09-17: the Gutenberg collection reads like books

The 1 TB standard: `gutenberg_en_all` (221 GB) and `survivorlibrary.com_en_all` (252 GB) moved from extended to core, so the core tier is about 680 GB and the external drive is now for Khan Academy, media and the owner's own files. README, the design spec's section 3 and the manifest tests (core ids, the `books` category, the 600 to 850 GB size bound) follow. Design in `docs/superpowers/specs/2026-09-14-gutenberg-library-design.md` with its "Amendments (2026-09-17)" section; plan in `docs/superpowers/plans/2026-09-17-gutenberg-books.md`.

- The real layout: the 2025-11 ZIM is gutenberg2zim 3.0.1's, checked against the scraper's tagged source. Its catalogue is JavaScript (`full_by_popularity.js`, rows of `[title, author, "hep" format flags, id, LCC shelf]`), book entries are `<title slug>.<id>` (the HTML article, no extension) and `<title slug>.<id>.epub`, covers `covers/<id>_cover_image.jpg`. The `books.json` the spec was written against exists only on the scraper's unreleased main branch; there are no subjects, author years or download counts anywhere in the file.
- `sos index` gains `index_books` (`api/sos/books.py`): it opens the ZIM with python-libzim (a new dependency; `install.sh` runs `sos index` before kiwix-serve is up, so the importer reads the file rather than the server), parses the catalogue, checks each book's EPUB and cover against the archive, and rewrites `books` and the external-content `fts_books` (rebuilt after every import, since FTS5 does not follow deletes). Absent ZIM, missing catalogue or malformed rows are warnings; the other steps still run. Tests build a six-book ZIM with libzim's writer.
- API (`api/sos/routers/books.py`): `GET /api/books` (search over title and author with bm25 weights 5 and 3, `?shelf=`, `?author=`, popular or title order, paged by 40 with a filtered `total`), `GET /api/books/shelves` (LCC codes with the scraper's own names), `GET /api/books/gutenberg/{id}` (with `available` and the saved position), and `GET/PUT/DELETE /api/reading[/{key}]`. The Library's Gutenberg card opens `/books`.
- Search: a `books` group from the catalogue (badge "Books", `kind: book`, weight 0.6), rendered below the guidance groups by the frontend's group order; the Kiwix fan-out skips the book ZIMs; suggest offers up to three catalogue titles.
- The reader: `EpubReader` takes a `memory` and saves the CFI plus an approximate percentage (spine position and page share, `web/src/reader/position.ts`) to the box two seconds after the last page turn; it reopens at the saved place and falls back to the start when the CFI no longer resolves. Library EPUBs get the same, keyed `doc:<id>`.
- Screens: `/books` (debounced search, shelf chips, Show more paging, "not on this box yet"), `/book/gutenberg/:id` (the reader; HTML-only books go to the Kiwix reader page; unavailable collection says so), and a "My books" shelf at the top of the Library with "42% read" and a Forget button.
- Not built: spec section 8 (semantic search, phase 2), the "scanned" badge for Survivor Library, author years.
- Review fixes (same day, from a branch review): the HTML entry name (the scraper stores the article without `.html`; the fixture had copied the same wrong rule), a `CROSS JOIN` so the catalogue search is planned FTS-first (the count query took seconds per keystroke on 70,000 rows), search and suggest gated on the ZIM being on the box, an un-prefixed suggest match so an author's name finds a book, `book` results kept out of the AI retriever's passages, the reader flushing its last page turn on close and coming back to the same page after the layout toggle, a stale-page guard on Show more, an unchanged-ZIM skip in `index_books`, and a free-space check in `sos sync` so a 680 GB core tier cannot fill a smaller drive.
- Verification: API suite 1356 passed; web suite 616 passed (77 files); `tsc --noEmit` clean; eslint only its two pre-existing warnings; `sos validate-playbooks --all-scenarios` OK, 131 documents. The real-ZIM acceptance run (plan Task 10) is recorded in the next entry.

## 2026-09-17: the real Gutenberg ZIM

The download finished at 16:00 and its sha256 matched the manifest. `zimdump list` confirmed the released scraper's layout: articles as `<slug>.<id>` with a `<slug>_cover.<id>` page beside each, `<slug>.<id>.epub`, `covers/<id>_cover_image.jpg`, the catalogue in `full_by_popularity.js` (five-field rows, Frankenstein first). `sos index` against the file took 27 seconds in total and reported **60,366 books**: 60,357 with an EPUB (9 flagged but absent), 60,109 with an HTML article, 60,277 with a cover, 60,306 on one of 39 shelves (American literature 11,188; English literature 9,654; Fiction and children's books 6,795). A second run skipped the unchanged file. Through the API, "robinson crusoe" returns 19 catalogue hits with Defoe's edition first, the same hit leads the Books search group and the suggest dropdown ("Robinson Crusoe — Daniel Defoe"), and kiwix-serve hands out the 4.9 MB EPUB, the cover and the article. On the dev stack with Playwright at the kiosk size: /books searched and opened the book, three page turns saved `gutenberg:70841` at 5%, a reload reopened at that CFI, and the Library's My books shelf listed it with its cover. The one thing that run exposed: a busy query's 40-result cap cut every low-scoring book hit while the group chip still counted them, so search now keeps up to five catalogue hits past the cap (`BOOKS_KEPT`). Hardware checklist rows 27 and 28 remain the owner's on the box.

## 2026-09-17: archived pages stop offering the internet

The owner: "in the wiki entries, can we remove links to things not in the library?" Checked first: mwoffliner already drops links to articles outside a ZIM's selection (every article link on "Burn", "Hypothermia" and "Water purification" resolves in both the medicine and the full Wikipedia ZIMs), so the dead links are the hundreds of internet links per page (citations, infoboxes, official sites; 265 on "Burn"), which until now stayed tappable and only produced the "Not in the library" notice. `unlinkExternal` in `web/src/links.ts` now runs on every frame load in the Reader and turns each of them into a plain `span.sos-unlinked`, wording kept; the click handler and notice remain for anchors a page script adds later. Verified on the dev stack with Playwright: "Burn" shows 264 unlinked spans, no remaining internet anchors, 891 article links untouched. Web suite 618 passed.

## 2026-09-17: the quick card uses the screen

The owner, looking at CPR, child and baby on a laptop: "why are all the medical cards not using the screen size, is that deliberate?" It was: `.card-sheet` stopped at 40em (640 px), the redesign's reading measure, which on the kiosk's 757 px content column wasted 117 px and on a 1400 px laptop left half the screen bare while the lead steps wrapped to two lines each. The sheet now fills the column up to 62em (about 1000 px): on the kiosk it takes the whole 725 px, on a laptop the three lead steps each sit on one line and the warnings and escalation follow the same width. Prose inside those panels keeps the 72-character measure. Checked on the dev stack at 853×480 and 1400×900; web suite 618 passed.

## 2026-09-17: the Library is a hub on the rail

The owner: "library should be on the left, guide should be part of library", then "a hub style with tiles". The rail's second destination is now Library (`/library`), lit for everything under it: the guides, the situations, modules, pages, tools, the reader, documents and books; Find keeps only search. The Library screen is a hub of three big tiles, each saying what is on its shelf: Guides ("41 pages and 6 tools, written for this box") opens `/library/guides`, the old Guides screen unchanged; Books ("60,366 books to read, Project Gutenberg", greyed with "not on this box yet" when the ZIM is absent) opens `/library/books`, the catalogue with the My books shelf now at its top; Collections ("192 items, 176 on this box") opens `/library/collections`, the old library list by category. `/guides` and `/books` redirect to their shelves; the Reader's "Open in library", the document viewer's library link and Find's category chips point at Collections; the Library card for Gutenberg opens `/library/books`. Measured on the dev stack: the hub has no page overflow at 853×480 and the rail's list has none either; the phone bar reads the same six names. Web suite 622 passed; tsc and eslint clean; the browser specs that named Guides on the rail now name Library.

## 2026-09-17: Sources, one tile per kind

The owner: "the collections page looks weak where it's all listed like that, also the tile is vague, what are collections". The third shelf is now Sources (`/library/sources`): the tile says "Wikipedia, the NHS, manuals, maps: 192 sources, 176 on this box", and the page is one tile per kind of source (UK official guidance, Medical, Survival, Reference, Practical and repair, Maps, Education, Books, Media, AI models), each with its icon, its count and two of its names ("29 sources, 28 on this box: NHS website, WikiMed and 27 more"), opening `/library/sources/<kind>` where the cards for that kind sit, scrolled to the one named in the hash. The 192-card wall (36,000 px tall on the kiosk) is gone; the hub is 220 px over one kiosk screen. `/library/collections` redirects; the reader's "Open in library" now lands on the item's own kind. Web suite 623 passed; tsc and eslint clean.

## 2026-09-17: the services row is icons

The owner: "on Now, for the icons at the bottom can they just be icons and if on or off, and when hovered it shows the name; also roads should be open/closed, and shops". The ten service buttons under the situations are now one touch target each with the icon alone: filled dark with ✕ when off, amber with ▲ when patchy, plain with a green ✓ when on, so the state is never colour alone. The name and state ("Roads closed") pop up over the button on hover or keyboard focus and are its accessible name throughout. A new `stateWord` in `web/src/situation/conditions.ts` says on/off for the utilities and open/closed for roads and shops, and the band chips, the board, the situation sheet and the briefing use the same words. Checked on the dev stack with water patchy and roads closed. Web suite 623 passed.
- Icons, same day ("the icons don't seem the best representation"): water is a tap, mobile is signal bars, heating a radiator, roads a road, shops a shop front, sewage a toilet (all new drawings in `icons.tsx` but the toilet); power keeps the bolt, landline the handset, internet the globe, gas the flame. The same icons follow the services onto the band, the board and the sheet.
- Words back, same day ("i need the words back just not as big"): the name sits under the icon in 11 px type and the mark moves to the button's corner; the hover pop-up went with the words visible again. Ten buttons sit on one row of the 757 px kiosk column (48 to 58 px each, none under the 48 px touch target); two rows on a phone.

## 2026-09-17: a tap on a service never alerts over a race

The owner: "when i click shops i get an alert?" The API log showed two taps answered 409 ("That condition changed while you were reading it"), which the front door turned into "Could not change Shops and cash: …". The screen's copy of the row was older than the box's (writes from another device or a test, or the tap before still settling). `flipCondition` in `web/src/situation/flip.ts` now carries the tap for the Now row and the board: on a 409 it re-reads the engine's view and sends the tap once more against the current timestamp, and a tap that finds the service already where it meant it is simply done. The situation sheet keeps its own "changed on another device; showing the latest" line, where the row's detail matters. Test: a 409 then a 200, no alert.
- The other alert, same evening ("the message doesn't make sense for shops"): tapping Shops off fires the `cash-only` consequence with `after: 0h`, and `ForecastReminders` treated it as a countdown that had run out: a toast ("▲ Cash only, and the machines are down. Card payments and cash machines fail together…") and the alarm sound, on the tap. Reminders now announce only countdowns the screen has watched running (`pendingIds` in `reminders.ts`, tracked by item so a rescheduled countdown still counts); a consequence due the moment it appears stays a line on the sheet. Test added.

## 2026-09-17: the clock on a guide, cookie banners, and the blank National Risk Register

- The clock control ("i don't like the start button and the changes after on it"): one slot in the screen head before and after. Before: a primary "Start the clock" with a clock icon, and one muted line under the summary saying what it does ("Start the clock and the tabs, the jobs and the forecasts follow the time"). After: the same slot is a quiet chip with the elapsed time ("just started", "5 min in", "2 h in"; the full "2 h in, first 72 hours" is its accessible name), which opens a small panel with the start time and "End situation" behind its confirm. The current phase is a dot in the corner of its tab instead of a badge, so the tab row keeps its shape. Measured on the kiosk: the head is 62 px before and after, the tabs stay on one row; it used to grow to 118 px and wrap the tabs.
- Cookie banners ("remove the cookies popups from the library items like gov uk"): the reader injects a third stylesheet, `DECLUTTER_CSS`, that hides the banners by their own class names (GOV.UK's `govuk-cookie-banner`, legislation.gov.uk's `cookie-preferences-banner`, the NHS's, and the usual consent widgets), never a wildcard on "cookie". Checked on prepare.campaign.gov.uk in the archive: the 360 px banner is gone.
- The National Risk Register opened blank: yesterday's fallback conversion left ten items with `kind: "pdf"` and `dest: docs/<id>.epub`, so the document screen handed PDF.js an EPUB. The ten are now `kind: "epub"` (their EPUBs exist and open, with the original PDF a toggle away), `library.file_url` follows the kind rather than the dest if the two ever disagree again, and a manifest test pins that a document's kind matches its file. Only the four OpenStax maths and physical-science titles remain PDF.
- Verification: API 1359 passed; web 625 passed; tsc and eslint clean.

## 2026-09-17: getting what is not on the box, and the cards of a kind of source

The owner: "there is a lot in library that is not installed, or not available; why, I can't even seem to add it", then "we need to make these sections look better". Sixteen items were absent on the PC for four different reasons and the card said "Not available" for all of them with nothing to do. Now `library_items` records each item's source (`source_type`, `source_tool`), `item_dict` says how an absent item gets here (`fetch`: `download` for a Kiwix or URL source the box can fetch itself, `drive` for an extended item with the drive unplugged, `build` for something made on a PC with `sos build-…`, `own` for the owner's own files), and the card acts on it: "Get it (2.1 GB)" for a download, which posts `/api/system/update` with `only: [id]` (the update runner passes `--only` to `sos sync`, the item's tier looked up; PIN-gated like a tier update), then "Getting it…" until the runner reports done and the library is re-read; a line saying what brings it otherwise. The card itself is redesigned: title beside its icon, a three-line description, the kind and drive badges with the size and date on one line, one action at the right, in a two-across grid on the kiosk (`.item-grid`; the card had no stylesheet at all before). Backend 1360 passed; web 627 passed; tsc and eslint clean.

## 2026-09-17: the Kit screens redesigned, and the Library's front filled

The owner: "the library has space under the tiles we should be using, also redesign the kit ui ux".

**Library.** Under the four shelf tiles: the books the household is in the middle of, as the same strip of covers the Books shelf has, and the twelve most-read books as covers with a way to all 60,366. The tiles still sit on the kiosk without scrolling; the rest is below.

**Kit, the front.** It was a paragraph, a stepper and fifteen wide tiles each carrying a sentence and "Basic 8/8 · Serious 2/9 · Full 0/6" in the warning colour: a long scroll that said nothing about the house as a whole. Now a panel at the top says where the house stands — the people stepper (a minus and a plus round the count) and one bar a tier, every kit's ticks added up: "Three days 13 of 97, Two weeks 8 of 103, No help coming 0 of 80" — over the kits as compact tiles, three or four to a row, each with its icon, one line of what it is, three small bars (one a tier, filled by how much of it is ticked) and a line in the words a household uses: "Three days: 0 of 5", "Three days ✓ · Two weeks: 2 of 9", "Everything packed". The tiers are said as how long they cover, never "basic, serious, full". On the kiosk the meters take a row of their own under the stepper.

**Kit, one kit.** The three tiers were three accordion panels with the first open. They are one switch now, three tabs each saying its count with a small bar, the first unpacked tier open, the tier's why under it; the other tiers stay on the page hidden, so the printer gets all three. A row is the tick, the name with its quantity in a pill at the end of the line, the reason under in smaller type, and the guide it comes from as a small button at the foot beside the ticked-by line. The head says "0 of 16 packed" beside who the quantities are for. Back goes to the kit list. Web 656 passed.

## 2026-09-17: the book follows the theme, and the field palette flipped

The owner: "if i change theme on a book the page doesnt change with the theme". Measured: after a tap on Theme the reader added Mono's stylesheet to the book — with Field's colours in it. The reader, the PDF viewer and the article reader all read the palette off the root when the theme changes, and React runs a child's effect before its parent's, so they read it a moment before the ThemeProvider had stamped the new theme on the root. The stamp is a layout effect now, which runs first, and the readers read the palette the theme has. Then the way back: epub.js adds the chosen theme's stylesheet to each chapter and leaves the last one's there, so Field's sheet, the older, lost to Mono's; the reader drops the other theme's sheets on a change. Checked both ways in the browser: white and ink, black and white, white and ink.

The same message: "i think the colours should be flipped, cream side bar and similar element that are currently white and the current cream goes white". Field's ground and panel have swapped: the ground is white and the rail, the band, the screen head, the reader's bar and every card are cream (dim likewise, the paler of its two on the ground). The book page is white. Print keeps its palette. Web 656 passed.

## 2026-09-17: Medical is a shelf of the Library

The owner: "medical should live in library". Medical had a row of its own on the rail; it is the first shelf of the Library now — its tile says "999, 30-odd quick cards, the NHS A to Z, children's doses" — and the rail has five destinations: Now, Library, Kit, Map, Find. The screen is the same (999, the quick cards in their groups, the NHS A to Z, the medical sources) at `/library/medical`, with Back to the Library; `/medical` sends there, and the cards and the doses keep their addresses (`/medical/card/<slug>`, `/medical/dose`), which the guides and the engine link to. Web 655 passed.

## 2026-09-17: the voice reads the books

The owner: "can the ai voice now read the books aloud now we converted them?" — and, while it was being built, "should auto scroll in the book with the reading". Piper and its British voice were already on the box for briefings and pages; what a book needed was a reading that starts where you are, goes on chapter after chapter, keeps the next piece ready while one plays, and takes the page with it. `web/src/reader/aloud.ts` walks the book's own files from the current place: the innermost blocks (a list item's paragraph once, not again as its item), folded into pieces — the first 200 characters so the voice begins within a couple of seconds on the Pi, then 600 — each with the CFI of its first paragraph, the next chapter loaded as the voice reaches it and let go of after. `speakFrom` in `tools/speech.ts` plays a sequence and asks the box for the next piece while this one plays (the page's Read aloud goes through it too). In the reader, Read aloud / Stop reading sit in the bar (hidden on a box with no voice), and as each piece is read the page scrolls smoothly to its paragraph, or turns to it in Pages. Leaving the book stops the voice. Checked on Robinson Crusoe: the voice began at the paragraph on the screen, the second piece was requested before the first had finished, the scroll followed piece by piece, and Stop gave the button back. Web 640 passed.
- The owner, on that first cut: "if i press read it reads all the crap like it reads all the chapters list front page etc, can really think about this feature and dont impliment it flipently, can we change voice settings?" Thought through with the owner (spec: docs/superpowers/specs/2026-09-17-read-aloud-books-design.md) and rebuilt. What is read: the furniture is taken out of each chapter first — Gutenberg's licence header and footer, cover wrapper, transcriber's notes and title pages, the contents and illustration tables and any table at all, figure captions and "(Larger)" links, footnote marks and page numbers, and any block that is mostly links — and a heading is a piece on its own with "CHAPTER I" said as "Chapter 1". Where it starts: from the front matter the voice jumps to chapter one (the first chapter-like heading in the first fifteen files, else the first file with three paragraphs), and the page goes with it; from inside the story, at the paragraph at the top of the screen. Playing: one audio element for the whole app, made on the tap and reused for every piece (Safari plays only what a touch started); Pause and Resume as well as Stop; the page follows the voice until you scroll it yourself, and Resume re-attaches it; each piece read is noted as the place to come back to ("if the books been read aloud then it should return top the last point it read" — checked: it does, page followed or not). Voice settings: `GET /api/voices` lists the single-speaker voices on the box; `POST /api/speak` takes `voice` and `speed` (Piper's `length_scale` is its inverse, 0.7–1.4) with a 0.3 s breath between sentences; a Voice row in the reader bar picks the voice and Slower / Normal / Faster, kept in the browser and applied to briefings and pages too. The catalogue offers Alan, Cori, Jenny and a northern English man at medium quality (63 MB each) and Cori at high ("why medium is that not the highest qulity?" — high is the one British voice Piper trained so, 114 MB, two to three times the compute) as `model` items in the ai category, fetched from Hugging Face with checksums, the box's sync fetching the `.onnx.json` beside each; Aru and Semaine are multi-speaker and not offered. Two faults found by reading Crusoe from the front in the browser: Gutenberg's files are XHTML, where `tagName` keeps the file's case, so the heading check against "H2" never matched (now `localName`); and a `<br/>` inside a heading joined "CHAPTER I" to the subtitle (now a space). Scrolling, the bar no longer shows Previous and Next. The owner, from an iPad Pro: "we limit the book width, whats the reason?" — and the fault behind it, "that was my issue when i changed the text size": the column was capped at 38em of the app's 16 px, 608 px, whatever the Text size, so at 150% a line fell to some 40 characters with a third of the screen empty either side. The cap now scales with the size — 608, 760 and 912 px — and a line is the same 58 characters at every setting, measured. Then "can we scale the text on the availible space? like on larger screen set the larger text size": the size a book opens at, when nobody on that device has chosen one, comes from the screen — the base 18 px under 1100 px across (the kiosk, the iPad mini), 125% from 1100, 150% from 1300 (an iPad Pro, a laptop) — and a choice made with Text size wins on that device thereafter. No cache of spoken audio: Piper on a Pi 5 is quicker than it plays and the next piece is fetched during this one. Backend 1380 passed; web 651 passed.

## 2026-09-17: reading a book, made to flow

The owner: "reading the books is still jarring and hard to flow". Measured on the iPad mini and the kiosk before anything changed: 196 and 190 px of furniture above the text (rail, band, screen head, the reader's bar), so the kiosk page was 290 px tall and turned every thirteen lines; 82 to 84 characters a line at 16 px; Gutenberg files in Times at 16 px with no space between paragraphs and a forced page break before every chapter heading; the text size forgotten from one book to the next. Three things, chosen from six offered:
- **Reading mode.** "Just the book" puts the rail, the band, the screen head and the bar away and the page has the whole screen (480 px on the kiosk, 768 on the iPad; the drill banner stays). A tap on the left or right of the page turns it, a tap on the middle brings the controls back, Escape too, and the arrow keys turn pages wherever the focus is. epub.js lays out for the host's size once and listens to the window, so a `ResizeObserver` on the host relays the new size to it, or the page stayed 290 px tall with the taps below it landing on nothing. The choice is remembered (`sos.reader.immersed`), and a toast the first time says how to get the controls back.
- **One typography for every book.** The reader's theme now sets the book's face aside for the box's: Source Serif 4 (the regular and italic weights, fetched at v14 to sit beside the 600 and 700 the app already had, and loaded into the frame from `/fonts/reader.css` since a frame has none of the app's stylesheets) at 18 px, leading 1.55, ragged right, hyphenated (the reflowed EPUBs now carry `lang="en"` so the browser will), a space between paragraphs, and no page of its own for a chapter heading. The column is 38em rather than 46em: 64 characters a line, measured. The Text size button's choice is remembered (`sos.reader.size`). The bar lost the words on Previous and Next (arrows, and now the taps and keys say the same) so it fits one row on the kiosk; "Original PDF layout" is "Original PDF".
- **Scrolling as well as pages.** A "Pages / Scrolling" button switches the reader between turning pages and one continuous scroll through the book (epub.js's continuous manager), reopening at the same place; the choice is remembered (`sos.reader.flow`). Scrolling, a tap on the page is only for the controls. The owner, having read both: "scrolling works much better or at least make that default" — so scrolling is the default and Pages the choice.
- The owner: "leaving a book and going back in doesnt always return you to exactly where you left". Measured: every reopening landed some 6.5 per cent further on. The book is laid out before its face has arrived, in the fallback serif, which is wider; the saved place is scrolled to in that layout, and when the face lands the text reflows shorter. The reader now waits for the frame's fonts and places the book again: three reopenings, the same paragraph each time, within a line. "still not working properly": two more causes found by testing a place near a chapter's start and a font delayed by 1.5 s. Scrolling, epub.js loads the chapters before the place in above it, each frame fetching its own copy of the face, and moves the scroll down by what they add — and the browser's scroll anchoring moved it too, for the same insertion, so the place came back a chapter early three times in four; `overflow-anchor: none` on the container leaves epub.js's arithmetic alone. And the wait for fonts was a wait for a stylesheet that had not yet arrived, so it resolved at once: the faces are now declared in the frame as rules, asked for by name and awaited, the files preloaded by the app. Scrolling, the place is then put right by hand — the remembered text measured, the scroll set to it — again whenever the layout moves, until it has been still for 0.7 s with every frame's faces in, and never once you have taken hold of the page. Eight reopenings in a row, chapter starts and delayed fonts among them, came back to the same line. On the Books shelf the "Most read / A to Z" order is the app's ordinary toggle buttons rather than pills ("these bubbles are not visually right").
Backend 1375 passed (57 EPUBs rebuilt with the language attribute); web 633 passed. Left for another day, from the same discussion: a progress line and a Contents button, opening a Gutenberg book at its first chapter rather than the licence, and the near-empty pages epub.js leaves before a picture that does not fit.

## 2026-09-17: the Books shelf as covers

The owner: "i need you to rework the ui ux on this section" (/library/books). The screen was one column of rows: every book a household had opened as a full-width row with an underlined title and a 48 px Forget button, so seven of them filled the iPad before the catalogue began; then a second search field under the head's "Search the box"; then 39 shelf chips; then the books, 40 × 60 covers beside a line of text. Now the front of the shelf is: **Continue reading** as a horizontal strip of covers with a progress bar along the foot of each and how far you are underneath (Forget is a small mark on the cover's corner); one search, the head's own gone from this screen; **Shelves** as twelve tiles with a count each and "All 39 shelves" behind a button; **Most read** as a grid of twelve covers with "All 60,366 books". A shelf, a search or All is its own screen, `/library/books?shelf=D`, `?q=prince`, `?all=1`, with the count, Most read / A to Z, a grid of covers and Show more; Back from a book returns to that screen rather than the front. A book without a cover — the box's own reflowed guides, the Gutenberg scans the scraper shipped without one, a cover file that fails to load — is drawn as a spine with its title on it, the same size as a cover, so neither the strip nor the grid has a hole. Covers are 132 px on the iPad and 104 px on the kiosk, where the strip's author line is also dropped, so a whole row fits under the head. Web 628 passed.
- Same evening, the owner on Robinson Crusoe: "what happens with the image on page 9". Project Gutenberg puts a "(Larger)" link under every illustration, pointing at a bigger copy of the picture; in the paginated reader it opens nothing and, being the only thing after a full-page picture, landed on a page of its own that said "(Larger)" and nothing else. The EPUB theme hides those links and keeps a figure on one page.

## 2026-09-17: the box's own reflow

The owner: "when i view some of the books we have reflowed they are terrible". Three faults, all Calibre's: each PDF line kept as a paragraph (57 to 84 per cent of "paragraphs" under 60 characters, running heads and page numbers in the text, hyphens unmended), two-column pages interleaved line by line (the NRR contents, the SCMG chapters with their font sizes mixed mid-sentence), and scans whose OCR layer is clipped or garbled reflowed anyway. `sos build-books` now uses `api/sos/reflow.py` instead of Calibre: `pdftotext` in reading order (columns in turn), running heads and page numbers dropped by repetition across pages (a three-line head, and a chapter title printed at the top of each of its pages, both caught), lines joined into paragraphs by punctuation, case and line width, hyphens mended except in compounds, headings from capitalised, numbered and "Chapter" lines (two-line titles merged; a numbered line among numbered lines is a step, not a heading), bullets as lists, and an EPUB 3 written directly (mimetype, container, package, nav, one XHTML per chapter, chapters cut at h1 and at h2 past 4,000 words, stubs folded). Two gates leave a book as PDF with no file behind: an OCR layer over 3 per cent garbled words, or a median paragraph under 25 characters after reflow (tables, lists, columns). Over the 72 candidates, 58 reflow (NRR median paragraph 226 characters, Where There Is No Doctor 75, the Ship Captain's Guide chapters 55 to 62, Kephart 32) and 14 go back to PDF and are `kind: "pdf"` with a URL source again: the RSGB band plan, Ofcom's IR2009 and amateur licence, the WHO essential medicines list, Approved Documents B1, M1 and O, the Wales resilience framework, NWSS, Seton, FM 21-76-1, FM 4-25-12, FM 31-70 and FM 3-25-26. The whole library converts in about 70 seconds; Calibre is no longer needed. Checked in the reader: the NRR and Where There Is No Doctor read as prose, the SCMG bullets hold together. Backend 1367 passed.
- Second pass, same evening, from the owner's iPad mini (1024 px wide, close to the box's own screen): the reader drew two pages side by side past 800 px, which read as the very columns the reflow had undone, so the EPUB reader now shows one page (`spread: 'none'`) in a column capped at 46em. In the reflow: contents pages (lines that run to a page number or across dot leaders) are dropped, as are lines that are only a dash or a few letters of OCR grit; a running head that alternates the book's title with the chapter's (left and right pages) is caught by looking four pages back; a figure caption ("Fig. 82—", or OCR's "Fig. r.—") floats out of the paragraph it interrupted and renders as a caption; a sentence crosses the blank a dropped running head leaves at the top of the next page; and a chapter is cut only at a line that says Chapter or Part, or at any heading once 4,000 words have run, so Kephart is 68 chapters rather than 605 and its median paragraph went from 32 to 78 characters (Where There Is No Doctor from 75 to 115). The SCMG annex went back to PDF (a list). Backend 1371 passed; web 627 passed.
- Third pass: a paragraph never starts with a lowercase letter, so one that would is joined to the paragraph before (this was the dominant fault everywhere: 42 per cent of Kephart's and the microbiology textbook's paragraphs began lowercase, cut by column and page gaps; Kephart's median paragraph went from 78 to 200 characters); a run-in small-capital subhead ("TENT FLOORS.—") opens its own paragraph; a title-case line inside a sentence that goes on underneath it ("End Joists Projecting so that Corner Stakes May Be Nailed to Them") is an unlabelled figure caption and floats out. Medians across the 57 now run from 52 to 340 characters. What remains in Kephart is the scan itself: text wrapped around drawings comes out of OCR interleaved with the caption, and small-capital heads are read as odd case. Backend 1373 passed.
- Fourth pass, from the owner's report that the NRR opened to an XML error ("PCDATA invalid Char value 8"): control characters in a PDF's text layer are stripped before the XHTML is written, and every EPUB is parse-checked after a build. On the same page the risk-themes list had swallowed the paragraph after it ("Conflict and instability The NRR assesses…") and had turned a mid-sentence dash ("– although several risks could be categorised") into a bullet: a dash inside an unfinished sentence now continues it, and a bulleted item ends when the next line starts with a capital after a short item line, unless the item hangs on a conjunction or preposition ("Cleaning of Cooking, Serving, and" / "Eating Utensils" stays one item). Across the 57 books that ended a few hundred lists that had run into their following paragraph; medians unchanged. Backend 1375 passed.
